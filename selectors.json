{
  "_comment": "Derived from a live probe run, not guessed. Yahoo's markup changes without notice - re-run `python -m waiver_window.probe --deep` and correct these. No code change needed.",
  "_note_search": "Yahoo ignores search= on the players page; every variant returned the full 120-row page. So the tool loads the available-player list and scans it client-side instead.",
  "available_page": "https://football.fantasysports.yahoo.com/f1/{league_id}/players?status=A&pos=O&sort=AR&count={offset}",
  "all_players_page": "https://football.fantasysports.yahoo.com/f1/{league_id}/players?status=ALL&pos=O&sort=AR&count={offset}",
  "roster_page": "https://football.fantasysports.yahoo.com/f1/{league_id}/{team_id}",
  "player_row": "table tbody tr",
  "player_name_cell": "a[href*='/nfl/players/']",
  "_note_status": "td[3] holds ownership. An owned player shows the fantasy team name linked to /f1/{league_id}/{team_number}. A free agent or waiver player shows a text label instead.",
  "player_status_cell": "td:nth-of-type(4)",
  "owner_team_link": "a[href*='/f1/{league_id}/']",
  "status_text_map": {
    "FA": "freeagents",
    "W ": "waivers",
    "W(": "waivers"
  },
  "page_size": 25,
  "max_scan_pages": 24,
  "submit_add": "input[type='submit'], button[type='submit']",
  "confirm_add": "input[value*='Confirm'], button:has-text('Confirm')",
  "_note_add": "Add links are signed with a per-session crumb token, so they are scraped from the row rather than constructed. Calibrate submit/confirm with `probe --add \"Player Name\"`.",
  "_note_paging": "Yahoo paginates the players table 25 at a time via &count=<offset>. The table also contains ~95 spacer/nested rows per page, so row count is not player count - only rows carrying a /nfl/players/ link are counted.",
  "addplayer_page": "https://football.fantasysports.yahoo.com/f1/{league_id}/addplayer?apid={player_id}",
  "addplayer_form": "form[action*='addplayer']",
  "drop_control": "input[name='dpid']",
  "_note_page_type": "Yahoo names the acquisition page in its heading. A player still on waivers renders 'Claim Player From Waivers'; a free agent renders an add page. This is an independent second gate, checked at the moment of action, on top of the status read from the list.",
  "claim_page_markers": [
    "claim player from waivers",
    "waiver claim",
    "claim player"
  ],
  "add_page_markers": [
    "add player",
    "add free agent"
  ],
  "render_timeout_ms": 8000,
  "_note_render": "Yahoo fills these tables after domcontentloaded, so every page load waits for real content before being indexed. Without that a slow render reads as an empty page and the scan stops early."
}
